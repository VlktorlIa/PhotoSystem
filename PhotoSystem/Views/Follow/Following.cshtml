@model IEnumerable<PhotoSystem.Models.Follow>

@{
    ViewData["Title"] = "Підписки";
}

<h2>Підписки</h2>

@if (!Model.Any())
{
    <p>Ви ще ні на кого не підписані</p>
}
else
{
    <table class="table">
        <tr>
            <th>Email</th>
            <th>Профіль</th>
        </tr>

        @foreach (var f in Model)
        {
            <tr>
                <td>@f.Following.Email</td>
                <td>
                    <a asp-controller="Users"
                       asp-action="Profile"
                       asp-route-id="@f.Following.Id"
                       class="btn btn-primary btn-sm">
                        Переглянути
                    </a>
                </td>
            </tr>
        }
    </table>
}